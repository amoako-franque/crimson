// Adding methods to an object!
const math = {
	numbers: [1, 2, 3, 4, 5],
	add: function (x, y) {
		return x + y
	},
	multiply(x, y) {
		return x * y
	},
}

// To execute mult //45iply:
math.multiply(5, 9)
